
/**
* this scenario might happen if a method call returns null for data and is more likely
 * where a block of scenarios get executed if there is data, otherwise they get ignored.
 */


where "we only have a where description", null, {

}

examples "we only have a example desc", null, {

}


where "we only have a where description", null

examples "we only have a example desc", null