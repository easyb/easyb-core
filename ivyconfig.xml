<ivysettings>
  <settings defaultResolver="chained"/>
  <resolvers>
    <chain name="chained" returnFirst="true">
      <filesystem name="libraries">
        <artifact pattern="${ivy.conf.dir}/repository/[organisation]/[revision]/[artifact]-[revision].[type]"/>
      </filesystem>
	<filesystem name="local-maven2" m2compatible="true">
	  <ivy pattern="${user.home}/.m2/repository/[organisation]/[module]/[revision]/[module]-[revision].pom"/>
	  <artifact pattern="${user.home}/.m2/repository/[organisation]/[module]/[revision]/[artifact]-[revision](-[classifier]).[ext]"/>
	</filesystem>
      <!--
      <url name="easybrepo">
        <artifact
            pattern="http://www.easyb.org/repo/[organisation]/[module]/[revision]/[artifact]-[revision].[ext]"/>
      </url>
      -->
      <ibiblio name="ibiblio" m2compatible="true"/>
      <!--<url name="ibiblio-mirror">-->
        <!--<artifact-->
            <!--pattern="http://mirrors.ibiblio.org/pub/mirrors/maven2/[organisation]/[module]/[branch]/[revision]/[branch]-[revision].[ext]"/>-->
      <!--</url>-->
      <!--<url name="ibiblio-mirror">-->
        <!--<artifact-->
            <!--pattern="http://repo1.maven.org/maven2/[organisation]/[module]/[revision]/[branch]-[revision].[ext]"/>-->
      <!--</url>-->
    </chain>
  </resolvers>
</ivysettings>