

// does this even make sense???
shared_specs "specs.shared"

it "should still be 20", {
  beforeVal.shouldBe 20
}

before "re-set the before", {
  beforeVal = 10
}

it "should now be 10", {
  beforeVal.shouldBe 10
}

